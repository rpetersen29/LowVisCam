<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- light theme -->
    <style name="AppBaseTheme" parent="@android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:actionMenuTextColor">@color/black</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
           parent="@android:Widget.Holo.ActionBar">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
    	<item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
    	<item name="android:actionOverflowButtonStyle">@style/OverFlow</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText"
           parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>
    
    <!-- ActionBar color -->
    <style name="OverFlow" parent="@android:style/Widget.Holo.ActionButton.Overflow">
    	<item name="android:src">@color/black</item>
	</style>
	
    <!-- dark theme -->
    <style name="AppBaseThemeDark" parent="@android:Theme.Holo">
        <item name="android:actionBarStyle">@style/MyActionBarDark</item>
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/yellow</item>
        <item name="android:actionMenuTextColor">@color/yellow</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBarDark"
           parent="@android:Widget.Holo.ActionBar">
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/yellow</item>
    	<item name="android:titleTextStyle">@style/MyActionBarTitleTextDark</item>
    	<item name="android:actionOverflowButtonStyle">@style/OverFlowDark</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleTextDark"
           parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/yellow</item>
    </style>
    
    <!-- ActionBar color -->
    <style name="OverFlowDark" parent="@android:style/Widget.Holo.ActionButton.Overflow">
    	<item name="android:src">@color/yellow</item>
	</style>
</resources>
